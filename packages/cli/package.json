{
  "name": "umbral",
  "version": "0.1.0",
  "description": "CLI para Umbral — gestiona config, cards, users, webhooks. Compatible con el API REST v1.",
  "type": "module",
  "bin": {
    "umbral": "./src/cli.ts"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "start": "tsx src/cli.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "tsx": "^4.7.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}